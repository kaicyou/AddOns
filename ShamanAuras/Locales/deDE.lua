-- these are the deDE localizations
-- we will use the Wowace localization system
-- since this isn't the default, check and exit if not deDE

local L = LibStub("AceLocale-3.0"):NewLocale("ShamanAuras", "deDE")

if not L then
	return
end
