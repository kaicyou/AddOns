-- these are the itIT localizations
-- we will use the Wowace localization system
-- since this isn't the default, check and exit if not itIT

local L = LibStub("AceLocale-3.0"):NewLocale("ShamanAuras", "itIT")

if not L then
	return
end
